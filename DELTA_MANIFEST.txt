VELO DELTA — build audit-v29
========================================
10 files. Apply: unzip -o into the repo ROOT.
Fixes: (1) Admin tab requires auth not just connected wallet; (2) hard 7-day
session TTL (auto-logout like a real exchange); (3) order book anon-REST
fallback so the fills tape always renders even in a stale-session state;
(4) redesigned logged-out trade overlay — clean, no ugly blur smear.
Verify after deploy: DevTools → Elements → <html data-velo-build="audit-v29">

FILES:
  PROJECT_STATUS.md
  SUPABASE_FIX_notifications_read.sql
  SUPABASE_MIGRATION_comments_v2.sql
  SUPABASE_MIGRATION_consolidated.sql
  SUPABASE_MIGRATION_notifications_activity.sql
  SUPABASE_MIGRATION_realtime_fix.sql
  src/App.tsx
  src/components/OrderBook.tsx
  src/components/ui/pages/TradeView.tsx
  src/index.tsx
